import { createTheme } from "@mui/material/styles";

export const theme = createTheme({
  palette: {
    primary: {
      main: "#faf8ef",
      dark: "#AD9D8F",
      light: "#cdc1b4"
    },
    text: {
      primary: "#958483",
      secondary: "#8c6e72"
    }
  }
});
